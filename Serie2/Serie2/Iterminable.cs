using System;
using System.Collections.Generic;
using System.Text;

namespace Serie2
{
    /// <summary>
    /// L'interface <interface>Iterminable</interface> contient la méthode qui permet de savoir si un utilisateur à gagné
    /// </summary>
    interface Iterminable
    {
        bool IsWon(char token);
    }
}
